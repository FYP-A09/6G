# Processed data

Empty for now. This is where cleaned/aligned tensors, graph snapshots, and
train/val/test splits derived from `data/raw/` will land once preprocessing
scripts exist in `src/data/`. Also gitignored — see `data/raw/README.md` for
the raw-data catalog this will be built from.
